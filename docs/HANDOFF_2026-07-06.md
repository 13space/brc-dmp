# BRC-DMP / BRC-LIFE 项目交接文档

交接日期：2026-07-06  
工作目录：`/Users/13space/Documents/数字物质理论DMT&Alife/brc-dmp`

## 1. 当前结论

当前工程已经从最初的 **BRC-DMP v0.1 数字物质资产协议原型**，推进到一个包含 **BRC-LIFE v1.0 / DMLP 数字物质生命协议原型** 的本地开发仓库。

目前有两条主线同时存在：

1. **BRC-DMP v0.1 资产协议线**
   - 任意资产上链的 DMO 数据模型。
   - RWA 样例资产。
   - AI Agent 身份钱包。
   - 交互证明记录。
   - Plutus 前端 MVP 和 API。

2. **BRC-LIFE v1.0 人工生命线**
   - `autopoietic_agent` / `constraint_edge` / `niche` / `species_genome`。
   - World Engine 计算生命状态，而不是由事件直接声明生命状态。
   - CoC³ 三值约束闭合向量。
   - 代谢、感知、行动、约束形成、复制、突变、凋亡。
   - population / Zipf / SOC / dark causality / PoUW / organic chain 实验测试。

截至本交接文档生成时，`npm test` 为 **60 个测试全部通过**。

## 2. 仓库状态

仓库已经在 `brc-dmp` 目录下初始化 Git，但还没有提交。当前 `git status --short` 显示所有工程文件仍为 untracked。

```text
?? .claude/
?? .env.example
?? .gitignore
?? README.md
?? apps/
?? docs/
?? fixtures/
?? package-lock.json
?? package.json
?? packages/
?? scripts/
?? services/
?? tests/
```

建议下一步先做一次审查，然后创建初始提交：

```bash
git add .
git commit -m "Initial BRC-DMP and BRC-LIFE protocol workspace"
```

## 3. 运行环境

`package.json` 当前版本：

```json
{
  "name": "brc-dmp",
  "version": "1.0.0-alpha.0",
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "dependencies": {
    "@scure/btc-signer": "^2.2.0"
  }
}
```

本地已经存在：

- `package-lock.json`
- `node_modules/`

如果换机器运行：

```bash
npm install
npm test
```

## 4. 核心命令

```bash
npm test
npm run validate
npm run index
npm run life
npm run life:pop
npm run api
npm run evolve
npm run adapt
npm run unify
npm run soc
npm run chain
npm run network
npm run csv
npm run csv:signet
npm run csv:signet:anchor
```

关键命令含义：

- `npm test`：运行全部测试，目前 60 个通过。
- `npm run validate`：验证 `fixtures/valid`、`fixtures/life`、`fixtures/population`，并确认 `fixtures/invalid` 被拒绝。
- `npm run index`：构建 v0.1/v1.0 兼容 DMO 状态。
- `npm run life`：运行 `fixtures/life` 的 BRC-LIFE World Engine。
- `npm run life:pop`：运行 `fixtures/population` 的 20 个群体样例。
- `npm run api`：启动本地 API 和 Plutus MVP 前端。

API 默认地址：

```text
http://127.0.0.1:8787
```

## 5. 当前验证结果

### 5.1 全量测试

最近一次结果：

```text
tests 60
pass 60
fail 0
duration_ms 1801.011292
```

测试覆盖方向：

- schema validation
- DMO indexer
- Agent DID wallet / RWA fixture
- World Engine
- evolution / adaptation
- SOC / Zipf
- organic chain
- network reorg
- PoUW
- CSV / Bitcoin OP_RETURN adapter
- Esplora offline stub
- signet anchor transaction construction

### 5.2 Fixture 校验

`npm run validate` 当前全部通过：

- `fixtures/valid`：12 个事件通过。
- `fixtures/life`：19 个事件通过。
- `fixtures/population`：20 个 population create 事件通过。
- `fixtures/invalid`：6 个无效样例均被正确拒绝。

### 5.3 DMO 索引结果

`npm run index` 当前输出摘要：

```text
protocol: brc-life
version: 1.0
asset_count: 4
event_count: 12
state_root: sha256:527033cdd7a4a58ca282be4cea87fca6a1b0045ca96078684c26b6d66f14d34f
```

资产：

- `dmo:plutus-indexer-agent-001`
  - `agent_identity`
  - Plutus Indexer Agent Genesis
  - 包含 DID Document、钱包绑定、密钥轮换、权限、行为边界、隐私策略。

- `dmo:rwa-autopoiesis-print-001`
  - `rwa_art`
  - Autopoiesis Diagram Print
  - 半真实 RWA 样例。

- `dmo:rwa-nickel-core-sample-001`
  - `rwa_art`
  - Nickel Core Provenance Sample
  - 半真实工业/矿物 RWA 样例。

- `dmo:the-one-rwa-001`
  - `rwa_art`
  - The One Sample Artifact
  - 初始 RWA 样例，已发生 transfer。

### 5.4 World Engine 结果

`npm run life` 当前输出摘要：

```text
tick 905010
population 3
alive 2
critical 0
dead 1
engine_root sha256:1f70576efe4ce941abb5df5f00ab9176a005bc36d83e62856f509eec51980446
state_root  sha256:c2a6d6f141ea15b2907d60a423b82bfd21d8c9ffa4241a52616a8e37fec6393f
```

生命个体：

- `dmo:genesis-cell-0001`
  - gen 0
  - born -> forage -> critical -> broken/dead

- `dmo:genesis-cell-0002`
  - gen 0
  - closed
  - spawn 了 `cell-0003`

- `dmo:genesis-cell-0003`
  - gen 1
  - mutation / membrane binding 后建立闭合并保持 closed

`npm run life:pop` 当前输出摘要：

```text
tick 905000
population 20
alive 20
critical 0
dead 0
engine_root sha256:85253e869b174b10a491d160805d69e53427c10e08d8e92528f453c85bea88bf
state_root  sha256:ddb398755c9f0eeb95d47e5b7092603a3d995610901939bd1afd93a33cf58755
```

population fixture 用于 Zipf / criticality / population-level 统计。

## 6. 协议版本与文档

### 6.1 v0.1

文件：

- `docs/protocol-v0.1-draft.md`
- `docs/architecture.md`
- `docs/vision.md`

v0.1 重点：

- `brc-dmp`
- `Digital Matter Object / DMO`
- RWA / Ordinals / DID / DAO / Agent identity
- 可复现索引器
- proof chain
- trust vector
- interaction proof
- Agent DID wallet

### 6.2 v1.0

文件：

- `docs/protocol-v1.0-draft.md`

v1.0 重点：

- `brc-life`
- `BRC-LIFE / DMLP`
- living digital matter object / LDMO
- autopoietic agent
- membrane
- metabolism
- genome: `M / R / phi`
- World Engine
- CoC³ 三值约束闭合
- closure verdict: `closed` / `critical_closed` / `broken`

注意：README 中部分文字仍有早期残留，例如测试数量处写过 “12 tests”，实际已经是 60 tests。建议后续顺手更新 README。

## 7. 目录结构

核心目录：

```text
apps/web/
  Plutus MVP 前端，直接由 services/api/server.js 托管。

docs/
  架构、愿景、v0.1 协议、v1.0 协议、交接文档。

fixtures/valid/
  v0.1 资产、RWA、Agent、证明、治理样例。

fixtures/life/
  BRC-LIFE 生命事件样例。

fixtures/population/
  20 个群体样例，用于 Zipf / population criticality。

fixtures/invalid/
  负样例，用于验证拒绝规则。

fixtures/templates/
  RWA onboarding 模板。

packages/schema/
  协议常量、校验器、canonical hash、JSON Schema 文档。

services/indexer/
  DMO 状态构建器。

services/api/
  本地 REST API 与前端静态文件服务。

services/world-engine/
  BRC-LIFE 生命状态计算、演化、适应、SOC、Zipf/unify 实验。

services/organic-chain/
  organic chain / PoUW / network reorg 实验。

services/csv-adapter/
  CSV/Bitcoin/OP_RETURN/signet adapter 实验。

scripts/
  population 生成、论文/图表数据导出、finite-size 等脚本。

tests/
  全量测试。
```

## 8. API 与前端

启动：

```bash
npm run api
```

打开：

```text
http://127.0.0.1:8787
```

现有 API：

- `GET /health`
- `GET /assets`
- `GET /assets/:id`
- `GET /assets/:id/proofs`
- `GET /assets/:id/trust`
- `GET /assets/:id/interactions`
- `GET /assets/:id/agent`
- `GET /assets/:id/did`
- `GET /agents`
- `GET /interactions`
- `GET /dao/summary`
- `GET /events`
- `GET /state-root`
- `GET /media/:file`

前端视图：

- Assets
- Agents
- Proofs
- DAO

前端目前是原生 HTML/CSS/JS，没有 React/Next.js。

## 9. Agent 身份钱包模型

Agent 样例：

```text
fixtures/valid/003-create-agent.json
fixtures/valid/008-bind-agent-wallet.json
fixtures/valid/009-rotate-agent-key.json
fixtures/valid/010-set-agent-policy.json
```

已实现能力：

- DID Document
- verification methods
- authentication / assertionMethod
- wallet binding
- wallet purpose: `control` / `fees` 等
- key rotation
- permission scopes
- behavior bounds
- interaction privacy

关键事件：

- `bind_wallet`
- `rotate_key`
- `set_agent_policy`
- `record_interaction`

当前 Agent DID：

```text
did:brc-dmp:plutus-indexer-agent-001
```

当前 active key：

```text
did:brc-dmp:plutus-indexer-agent-001#key-2
```

## 10. RWA 样例与模板

RWA 样例：

- `fixtures/valid/001-create-rwa.json`
- `fixtures/valid/011-create-rwa-autopoiesis-print.json`
- `fixtures/valid/012-create-rwa-mineral-sample.json`

视觉素材：

- `apps/web/media/the-one-sample-artifact.svg`
- `apps/web/media/rwa-autopoiesis-print.svg`
- `apps/web/media/rwa-nickel-core-sample.svg`

模板：

- `fixtures/templates/rwa-asset-template.json`

每个 RWA 样例已经包含：

- owner address
- metadata URI/hash
- image URI
- certificate URI
- proof hash
- risk summary
- trust vector

这些仍然是 fixture / semi-real 样例，不能当作真实法律凭证。

## 11. BRC-LIFE / World Engine 现状

World Engine 代码：

```text
services/world-engine/src/engine.js
services/world-engine/src/evolve.js
services/world-engine/src/adapt.js
services/world-engine/src/soc.js
services/world-engine/src/unify.js
services/world-engine/src/zipf.js
```

World Engine 当前计算：

- metabolism energy ledger
- membrane binding
- genome triad `M / R / phi`
- CoC³ 三值约束闭合
- closure verdict
- spawn / mutation / apoptosis
- deterministic `engine_root`

v1.0 文档里明确：

- 生命状态由 World Engine 计算，不由事件声明。
- C2 / C3 / C5 仍有 pending conditions。
- 不声称现象意识或强涌现。
- 不做链上任意代码执行。

## 12. Organic Chain / PoUW / Network 实验

相关目录：

```text
services/organic-chain/
tests/organic-chain.test.js
tests/network.test.js
```

已测试能力：

- mining deterministic
- chain validates itself
- world engine in consensus
- difficulty self-tuning
- useful-work accumulation
- honest nodes gossip and converge
- partition fork
- healing reorg
- reorg recomputes living-world state deterministically
- PoUW hard to produce / cheap to verify

这是 v2.0 方向的研究原型：把 World Engine / useful work 融入链式共识实验。

## 13. Bitcoin / CSV / Signet Adapter

相关目录：

```text
services/csv-adapter/
tests/csv-adapter.test.js
tests/esplora.test.js
tests/signet-anchor.test.js
```

当前覆盖：

- OP_RETURN commitment round-trip
- real signet OP_RETURN push parser
- Esplora spend following offline stub
- Bitcoin-linked seal chain
- tamper rejection
- forged history rejection
- double-spend rejection
- signet anchor tx construction
- WIF network guard

这部分是“真实链上事件适配器”的基础，但仍主要是本地/离线/构造交易层面的验证，不等于已接入线上 signet 服务。

## 14. 当前主要风险

1. **所有代码尚未 Git commit**
   - 当前仓库可运行，但还没有稳定提交点。

2. **README 有过时内容**
   - 测试数量和目录说明没有完全同步最新 v1.0。

3. **没有 CI**
   - 当前验证依赖本地 `npm test`。

4. **真实链上接入仍未完成**
   - CSV/signet adapter 有基础，但还没有持续扫描 Bitcoin/Ordinals 事件并写入 indexer 的正式服务。

5. **JSON Schema 与手写 validator 并存**
   - 当前 runtime validation 主要靠 `packages/schema/src/validate.js`。
   - JSON Schema 更像文档和未来兼容层。

6. **RWA 仍是样例，不是合规产品**
   - 没有真实 custody、鉴定、法律文件、KYC/AML、交易牌照逻辑。

7. **World Engine 是确定性研究原型**
   - 它是人工生命工程基座，但不是“已证明真正生命/意识”。

## 15. 建议下一步

优先级从高到低：

1. **做第一次 Git commit**
   - 保留当前 60 tests pass 的状态。

2. **更新 README**
   - 同步 v1.0、60 tests、完整目录和命令。

3. **添加 CI**
   - GitHub Actions 或本地脚本均可，至少跑：
     - `npm test`
     - `npm run validate`
     - `node --check apps/web/app.js`

4. **整理 protocol-v1.0 与代码的一致性**
   - 确认所有 v1.0 op / asset kind / life fields 在文档、validator、fixtures、tests 中一致。

5. **推进真实链上事件适配器**
   - 目标：Bitcoin / Ordinals / OP_RETURN / inscription payload -> BRC-DMP/BRC-LIFE event -> indexer state。

6. **把 Plutus 前端从原生 JS 升级为正式 app**
   - 可选 Next.js / Vite。
   - 保留当前原生版本作为最小 fallback。

7. **把 World Engine 结果暴露到 API/前端**
   - `/life`
   - `/life/:id`
   - `/life/engine-root`
   - 前端增加 Life / Population / Zipf / SOC 页面。

8. **RWA onboarding 流程化**
   - 根据 `fixtures/templates/rwa-asset-template.json` 做 CLI 或表单生成 fixture。

9. **Agent wallet 签名验证**
   - 从声明模型走向真实 BIP-322 / Schnorr / WIF/testnet 签名验证。

10. **把 fixtures 分层**
   - `fixtures/assets`
   - `fixtures/agents`
   - `fixtures/rwa`
   - `fixtures/life`
   - `fixtures/population`
   - 避免 v0.1/v1.0 后续混杂。

## 16. 交接快速启动

从零开始接手：

```bash
cd "/Users/13space/Documents/数字物质理论DMT&Alife/brc-dmp"
npm install
npm test
npm run validate
npm run index
npm run life
npm run api
```

检查前端：

```text
http://127.0.0.1:8787
```

检查 API：

```bash
curl http://127.0.0.1:8787/health
curl http://127.0.0.1:8787/assets
curl http://127.0.0.1:8787/agents
curl http://127.0.0.1:8787/dao/summary
```

推荐第一件事：

```bash
npm test
git status --short
git add .
git commit -m "Initial BRC-DMP and BRC-LIFE protocol workspace"
```

## 17. 一句话交接

这个仓库当前已经不是单纯的 RWA / Agent 身份协议草稿，而是一个 **Bitcoin-rooted digital matter + Agent wallet + RWA proof + deterministic artificial-life world engine** 的综合原型。  
它的强项是：协议表面、fixture、状态机、World Engine 和测试都已经能跑通。  
它的下一道门槛是：把本地 fixture 世界接到真实 Bitcoin / Ordinals / Signet 事件流上，并把 v1.0 生命层结果纳入 API 与前端展示。
